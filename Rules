#!/usr/bin/env ruby

compile '/' do
  filter :haml
  layout 'default'
end

compile '*' do
  filter 'coderay'
  filter 'bluecloth'
  layout 'article'
  layout 'default'
end

route '*' do
  item.identifier + 'index.html'
end

layout '*', :haml, :format => :html5
